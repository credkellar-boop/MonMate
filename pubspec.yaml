dependencies:
  web3dart: ^2.7.0
  http: ^1.1.0
  walletconnect_dart: ^0.0.11 # For connecting MetaMask/Rabby
dependencies:
  # Real-time Video & Audio
  livekit_client: ^2.0.0 
  # Messaging, Presence, and Online Status
  supabase_flutter: ^2.0.0
  # Permissions (Camera/Mic)
  permission_handler: ^11.0.0
