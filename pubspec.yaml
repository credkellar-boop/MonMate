uses: subosito/flutter-action@v2
with:
  channel: stable

# Ensure your standard Flutter configuration (assets, material design) remains below if present:
flutter:
  uses-material-design: true
flutter pub get
flutter analyze
flutter test
